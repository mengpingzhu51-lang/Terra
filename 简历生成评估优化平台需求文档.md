# 简历生成 / 评估 / 优化平台需求文档

## 1. 文档信息

| 项目 | 内容 |
|---|---|
| 产品名称 | 简历生成 / 评估 / 优化平台 |
| 文档版本 | v0.1 |
| 端类型 | Web 网页端优先 |
| MVP 语言 | 中文简历 |
| 登录方式 | 手机号登录 |
| 首期模板 | 2-3 个简历模板风格 |

---

## 2. 项目背景

求职者在制作简历和投递岗位时，通常面临以下问题：

1. 不知道如何从零开始组织简历内容。
2. 已有简历内容散乱，缺少结构化表达。
3. 同一份简历难以适配不同岗位 JD。
4. 不清楚简历与目标岗位的匹配度。
5. 缺少具体、可执行的简历优化建议。
6. 针对不同 JD 手动修改简历成本较高。

本平台希望通过简历生成、JD 管理、简历评估和一键优化能力，帮助用户快速生成可投递的中文简历，并针对不同岗位生成定制化简历版本。

---

## 3. 产品目标

### 3.1 核心目标

1. 支持用户从零创建中文简历。
2. 支持用户上传已有 PDF / Word 简历并生成结构化内容。
3. 支持将简历先渲染为 HTML，再导出为 PDF。
4. 支持用户管理目标岗位 JD。
5. 支持选择 JD 和简历进行匹配评估。
6. 支持输出评分、问题分析和优化建议。
7. 支持一键生成针对 JD 优化后的简历副本。
8. 支持保存每次 AI 生成、评估和优化的历史记录。

### 3.2 MVP 范围

MVP 优先实现 Web 网页端，不考虑完整移动端。

首期只支持：

- 中文简历
- 手机号登录
- PDF / Word 简历上传
- 2-3 个简历模板风格
- 简历 HTML 预览
- PDF 导出
- JD 创建、管理、评估
- AI 优化建议
- 一键生成优化简历副本
- 历史记录保存

---

## 4. 用户角色

## 4.1 普通用户

主要用户为求职者。

用户目标：

- 创建简历
- 上传并优化已有简历
- 针对目标 JD 评估简历匹配度
- 获得简历修改建议
- 生成不同岗位方向的简历版本
- 导出 PDF 简历用于投递

## 4.2 平台管理员，后续阶段

管理员能力不进入 MVP，后续可支持：

- 模板管理
- AI 提示词配置
- 用户反馈管理
- 文件风控管理
- 数据统计分析

---

## 5. 核心业务流程

## 5.1 手机号登录流程

```text
用户进入 Web 端
→ 输入手机号
→ 获取验证码
→ 输入验证码
→ 验证成功
→ 进入工作台
```

要求：

- 首期仅支持手机号登录。
- 不支持邮箱登录、账号密码登录、第三方登录。
- 新手机号首次登录时自动创建用户账号。
- 用户登录后只能访问自己的简历、JD、评估记录和生成记录。

---

## 5.2 创建简历流程

```text
进入工作台
→ 点击创建简历
→ 选择手动填写 / 上传附件
→ 系统生成结构化简历内容
→ 用户编辑确认
→ 选择简历模板
→ 生成 HTML 简历预览
→ 保存简历
→ 可导出 PDF
```

---

## 5.3 JD 评估流程

```text
进入 JD 管理
→ 新增 JD
→ 输入或上传 JD 内容
→ 系统解析 JD
→ 选择一份简历
→ 发起评估
→ 查看评分、分析和优化建议
```

---

## 5.4 一键优化流程

```text
进入评估结果页
→ 点击一键生成优化简历
→ 系统基于原简历、JD、评估结果生成优化副本
→ 用户查看优化后简历
→ 用户继续编辑
→ 重新评估或导出 PDF
```

要求：

- 原简历不被覆盖。
- 优化结果生成新的简历副本。
- 新简历需要关联原简历、目标 JD 和评估记录。
- AI 不应凭空编造用户经历。
- 无法确认的信息应提示用户补充或确认。

---

## 6. 功能需求

# 6.1 登录体系

## 6.1.1 手机号登录

用户通过手机号验证码登录。

### 功能要求

- 输入手机号。
- 获取短信验证码。
- 输入验证码完成登录。
- 验证码有效期限制。
- 验证码错误提示。
- 验证码发送频率限制。
- 登录成功后进入工作台。
- 首次登录自动创建用户。

### 暂不支持

- 邮箱登录
- 账号密码登录
- 微信登录
- GitHub 登录
- 企业 SSO

---

# 6.2 工作台

## 6.2.1 页面目标

工作台用于展示用户的核心资产和快捷操作入口。

## 6.2.2 页面内容

展示：

- 我的简历
- 我的 JD
- 最近评估记录
- 最近生成记录
- 快捷入口

快捷入口包括：

- 创建简历
- 上传简历
- 新增 JD
- 发起评估

---

# 6.3 简历管理

## 6.3.1 创建简历

用户可以通过以下方式创建简历：

1. 手动填写结构化信息。
2. 上传 PDF / Word 简历解析生成。
3. 复制已有简历生成新版本。
4. 基于 JD 优化结果生成副本。

## 6.3.2 简历字段

简历首期支持以下模块：

| 模块 | 字段 |
|---|---|
| 基本信息 | 姓名、手机号、邮箱、城市、求职意向、个人链接 |
| 教育经历 | 学校、学历、专业、起止时间、经历描述 |
| 工作经历 | 公司、职位、起止时间、工作内容、工作成果 |
| 项目经历 | 项目名称、角色、起止时间、项目背景、个人职责、项目成果 |
| 技能清单 | 专业技能、工具、语言、证书 |
| 个人总结 | 个人优势、职业亮点 |
| 自定义模块 | 自定义标题、自定义内容 |

## 6.3.3 简历编辑

用户可以编辑结构化简历内容。

支持能力：

- 新增模块
- 删除模块
- 调整模块顺序
- 编辑模块内容
- 保存草稿
- 选择模板
- 预览 HTML 简历
- 导出 PDF

## 6.3.4 简历版本

每份简历可以有多个版本。

版本类型：

| 类型 | 说明 |
|---|---|
| original | 原始简历 |
| manual_copy | 手动复制版本 |
| jd_optimized | 针对 JD 优化版本 |

每个版本需要记录：

- 简历 ID
- 用户 ID
- 简历名称
- 来源简历 ID
- 关联 JD ID
- 关联评估 ID
- 版本类型
- 创建时间
- 更新时间

---

# 6.4 简历生成

## 6.4.1 手动输入生成

用户填写结构化简历信息后，系统生成 HTML 简历。

要求：

- 内容结构清晰。
- 表达适合招聘阅读习惯。
- 支持用户继续编辑。
- 支持模板选择。
- 支持 PDF 导出。

## 6.4.2 上传附件生成

用户可以上传已有简历文件，系统解析内容并生成结构化简历。

首期支持格式：

- PDF
- Word，包含 `.doc` 和 `.docx`

上传要求：

- 限制文件大小。
- 限制文件格式。
- 文件上传失败时支持重试。
- 解析失败时提示用户手动补充。
- 解析结果必须进入可编辑状态，由用户确认后保存。

## 6.4.3 HTML 简历生成

系统默认先生成 HTML 简历。

要求：

- 适配 Web 预览。
- 适合 A4 打印。
- 中文排版清晰。
- 支持分页预览。
- 支持 2-3 个模板风格。
- 支持后续 PDF 导出。

## 6.4.4 简历模板

首期提供 2-3 个模板风格。

建议模板：

| 模板 | 定位 |
|---|---|
| 简洁专业版 | 适合互联网、产品、运营、职能岗位 |
| 技术工程版 | 适合研发、算法、数据、工程类岗位 |
| 重点突出版 | 适合项目经历丰富、希望突出成果的用户 |

模板要求：

- 均支持中文内容。
- 均支持 HTML 预览。
- 均支持 PDF 导出。
- 用户可以切换模板，不影响结构化简历内容。

## 6.4.5 PDF 导出

用户可以将 HTML 简历导出为 PDF。

要求：

- 默认 A4 尺寸。
- 保持 HTML 视觉效果。
- 支持中文字体。
- 支持文件命名。
- 导出失败时提示用户重试。
- 导出的 PDF 可用于正式投递。

---

# 6.5 JD 管理

## 6.5.1 创建 JD

用户可以通过以下方式创建 JD：

- 手动输入 JD 文本。
- 粘贴招聘网站 JD 内容。
- 上传 JD 附件，后续可选。

## 6.5.2 JD 字段

| 字段 | 说明 |
|---|---|
| JD 标题 | 岗位名称 |
| 公司名称 | 可选 |
| JD 原文 | 用户输入或上传的完整 JD 内容 |
| 岗位职责 | 系统解析或用户编辑 |
| 任职要求 | 系统解析或用户编辑 |
| 技能关键词 | 系统提取 |
| 经验要求 | 系统提取 |
| 学历要求 | 系统提取 |
| 创建时间 | 系统生成 |
| 更新时间 | 系统生成 |

## 6.5.3 JD 列表

列表展示：

- JD 标题
- 公司名称
- 核心关键词
- 创建时间
- 最近评估时间
- 已关联简历数量

支持操作：

- 查看
- 编辑
- 删除
- 选择简历评估

## 6.5.4 JD 详情

详情展示：

- JD 原文
- 岗位职责
- 任职要求
- 核心关键词
- 历史评估记录

支持操作：

- 编辑 JD
- 选择简历评估
- 查看历史评估

---

# 6.6 简历评估

## 6.6.1 发起评估

用户选择：

- 一份 JD
- 一份简历

系统对简历和 JD 进行匹配评估。

## 6.6.2 评估维度

首期按以下维度评估：

| 维度 | 说明 |
|---|---|
| 总体匹配度 | 简历与 JD 的整体匹配情况 |
| 技能匹配度 | 技能关键词是否覆盖 JD 要求 |
| 经验匹配度 | 工作经历 / 项目经历是否符合岗位要求 |
| 行业匹配度 | 是否体现相关行业、业务或场景经验 |
| 职责匹配度 | 过往职责是否贴合 JD 中的职责描述 |
| 成果表达 | 是否有量化成果、业务影响和结果导向表达 |
| 缺失信息 | JD 需要但简历中未体现的信息 |
| 风险项 | 年限、学历、技能、行业经验等潜在不匹配项 |

## 6.6.3 评估结果

评估结果需要包括：

- 总分，0-100 分。
- 各维度评分。
- 优势分析。
- 不足分析。
- 关键词覆盖情况。
- 缺失关键词。
- 风险项。
- 可优化模块。
- 具体修改建议。

## 6.6.4 关键词分析

系统从 JD 中提取关键词，并与简历内容进行匹配。

关键词类型：

- 专业技能
- 工具平台
- 业务领域
- 岗位职责
- 软技能
- 证书
- 学历
- 工作年限

输出示例：

| 关键词 | JD 是否要求 | 简历是否体现 | 建议 |
|---|---|---|---|
| React | 是 | 是 | 保留，并强化项目中的使用场景 |
| 数据分析 | 是 | 否 | 如有相关经历，建议补充项目或工作案例 |
| 跨部门协作 | 是 | 弱体现 | 在工作经历中补充协作对象和结果 |

---

# 6.7 简历优化建议

## 6.7.1 建议类型

系统应输出具体、可执行的建议。

建议类型包括：

1. 内容补充：补充技能、项目背景、业务场景。
2. 表达优化：将职责描述改为结果导向表达。
3. 关键词增强：合理加入 JD 中的重要关键词。
4. 结构调整：将更相关的经历提前展示。
5. 删除弱相关内容：减少与目标岗位无关的内容篇幅。
6. 量化成果：增加效率提升、成本降低、转化率、用户规模等指标。

## 6.7.2 建议展示方式

建议按简历模块展示。

示例：

```text
模块：工作经历 - XX 公司
问题：当前描述偏职责罗列，缺少与 JD 中“数据驱动决策”的关联。
建议：补充你在数据分析、指标监控、策略验证方面的具体成果。
推荐改写：负责用户增长数据分析，搭建核心指标看板，辅助业务团队定位转化漏斗问题，使注册转化率提升 18%。
```

---

# 6.8 一键生成 JD 优化简历副本

## 6.8.1 生成依据

系统基于以下内容生成优化简历副本：

- 原始简历
- JD 内容
- JD 解析结果
- 简历评估结果
- 优化建议

## 6.8.2 生成规则

- 不覆盖原简历。
- 生成新的简历副本。
- 新简历关联目标 JD。
- 新简历关联评估记录。
- 新简历可以继续编辑。
- 新简历可以再次评估。
- 新简历可以导出 PDF。
- AI 不能凭空编造经历、学历、公司、项目或技能。
- 如果需要用户确认的信息，应以待补充提示呈现。

## 6.8.3 生成后能力

用户可以：

- 查看优化后的简历。
- 编辑优化后的内容。
- 查看与原简历的差异，P1。
- 重新发起 JD 评估。
- 导出 PDF。

---

# 6.9 历史记录

## 6.9.1 AI 生成历史

需要保存每次 AI 生成记录。

记录内容：

- 用户 ID
- 生成类型
- 输入内容摘要
- 输出内容
- 关联简历 ID
- 关联 JD ID
- 创建时间
- 生成状态
- 错误信息，若失败

生成类型包括：

- resume_parse：上传简历解析
- resume_generate：简历生成
- jd_parse：JD 解析
- evaluation：简历评估
- optimization_suggestion：优化建议生成
- jd_resume_optimize：JD 定制简历生成

## 6.9.2 评估历史

每次评估都需要保存。

记录内容：

- 评估 ID
- 用户 ID
- 简历 ID
- JD ID
- 总分
- 分项评分
- 优势分析
- 不足分析
- 关键词分析
- 优化建议
- 创建时间

## 6.9.3 优化历史

每次一键优化都需要保存。

记录内容：

- 优化记录 ID
- 用户 ID
- 原简历 ID
- 新简历 ID
- JD ID
- 评估 ID
- 优化摘要
- 创建时间

---

## 7. 页面需求

# 7.1 登录页

功能：

- 输入手机号
- 获取验证码
- 输入验证码
- 登录 / 注册
- 错误提示

---

# 7.2 工作台

展示：

- 简历数量
- JD 数量
- 最近评估记录
- 最近生成记录
- 快捷操作入口

---

# 7.3 简历列表页

展示：

- 简历名称
- 模板风格
- 版本类型
- 关联 JD
- 最近更新时间

操作：

- 查看
- 编辑
- 复制
- 导出 PDF
- 发起 JD 评估
- 删除

---

# 7.4 简历编辑页

功能：

- 编辑结构化内容
- 调整模块顺序
- 选择模板
- 实时预览 HTML
- 保存
- 导出 PDF
- 发起评估

---

# 7.5 简历预览页

展示：

- HTML 简历预览
- 当前模板
- PDF 导出按钮
- 编辑入口

---

# 7.6 JD 列表页

展示：

- JD 标题
- 公司名称
- 核心关键词
- 创建时间
- 最近评估时间

操作：

- 查看
- 编辑
- 删除
- 选择简历评估

---

# 7.7 JD 详情页

展示：

- JD 原文
- 岗位职责
- 任职要求
- 核心关键词
- 历史评估记录

操作：

- 编辑 JD
- 选择简历评估
- 查看评估结果

---

# 7.8 评估结果页

展示：

- 总体评分
- 分项评分
- 优势分析
- 不足分析
- 关键词匹配情况
- 风险项
- 优化建议
- 一键生成优化简历按钮

操作：

- 生成优化简历副本
- 返回编辑原简历
- 重新评估

---

# 7.9 历史记录页

展示：

- 简历生成历史
- 简历解析历史
- JD 解析历史
- 评估历史
- 优化历史

支持筛选：

- 类型
- 时间
- 关联简历
- 关联 JD

---

## 8. 数据模型草案

## 8.1 User

```ts
type User = {
  id: string
  phone: string
  createdAt: string
  updatedAt: string
}
```

## 8.2 Resume

```ts
type Resume = {
  id: string
  userId: string
  title: string
  sourceResumeId?: string
  relatedJdId?: string
  relatedEvaluationId?: string
  templateId: string
  content: ResumeContent
  html?: string
  pdfUrl?: string
  versionType: 'original' | 'manual_copy' | 'jd_optimized'
  createdAt: string
  updatedAt: string
}
```

## 8.3 ResumeContent

```ts
type ResumeContent = {
  basicInfo: BasicInfo
  education: Education[]
  workExperience: WorkExperience[]
  projects: ProjectExperience[]
  skills: string[]
  summary?: string
  customSections?: CustomSection[]
}
```

## 8.4 JobDescription

```ts
type JobDescription = {
  id: string
  userId: string
  title: string
  company?: string
  rawContent: string
  responsibilities?: string[]
  requirements?: string[]
  keywords?: string[]
  experienceRequirement?: string
  educationRequirement?: string
  createdAt: string
  updatedAt: string
}
```

## 8.5 Evaluation

```ts
type Evaluation = {
  id: string
  userId: string
  resumeId: string
  jdId: string
  totalScore: number
  dimensionScores: {
    skills: number
    experience: number
    responsibility: number
    industry: number
    achievement: number
  }
  strengths: string[]
  weaknesses: string[]
  risks: string[]
  keywordMatches: KeywordMatch[]
  missingKeywords: string[]
  suggestions: OptimizationSuggestion[]
  createdAt: string
}
```

## 8.6 OptimizationSuggestion

```ts
type OptimizationSuggestion = {
  section: string
  issue: string
  suggestion: string
  exampleRewrite?: string
}
```

## 8.7 GenerationRecord

```ts
type GenerationRecord = {
  id: string
  userId: string
  type:
    | 'resume_parse'
    | 'resume_generate'
    | 'jd_parse'
    | 'evaluation'
    | 'optimization_suggestion'
    | 'jd_resume_optimize'
  inputSummary?: string
  outputContent?: unknown
  resumeId?: string
  jdId?: string
  evaluationId?: string
  status: 'success' | 'failed'
  errorMessage?: string
  createdAt: string
}
```

---

## 9. 非功能需求

## 9.1 性能

- Web 页面常规操作应快速响应。
- HTML 简历生成应在可接受时间内完成。
- PDF 导出过程需要展示加载状态。
- AI 评估和优化过程允许较长等待，但必须展示进度或等待提示。

## 9.2 安全

- 用户只能访问自己的数据。
- 手机号登录需要验证码频率限制。
- 上传文件需要限制格式和大小。
- PDF / Word 解析过程不得执行文件内潜在脚本。
- 用户简历、手机号、JD 等隐私数据需要安全存储。
- 导出的 PDF 地址需要权限控制。

## 9.3 稳定性

- AI 生成失败时应给出明确错误提示。
- 文件上传失败时允许重试。
- 文件解析失败时允许用户手动填写。
- PDF 导出失败不影响已保存简历内容。
- 评估失败不影响原简历和 JD 数据。

## 9.4 可编辑性

- AI 生成的简历内容必须允许用户编辑。
- AI 解析的 JD 内容必须允许用户编辑。
- 一键优化后的简历必须允许用户二次修改。

## 9.5 可追溯性

系统应保留：

- 原始简历
- 上传解析记录
- AI 生成记录
- JD 原文
- 评估记录
- 优化建议
- 优化后的简历版本

---

## 10. MVP 优先级

## 10.1 P0 必须实现

- 手机号登录
- Web 工作台
- 手动创建中文简历
- 上传 PDF / Word 简历
- 简历解析为结构化内容
- 简历结构化编辑
- 2-3 个简历模板
- HTML 简历预览
- PDF 导出
- JD 创建与管理
- JD 解析与关键词提取
- 选择 JD + 简历进行评估
- 输出评分、分析和优化建议
- 一键生成 JD 优化简历副本
- 保存 AI 生成、评估和优化历史记录

## 10.2 P1 重要但可后置

- 优化前后 diff 对比
- JD 关键词可视化
- 评估历史趋势
- 更多简历模板
- 多版本对比
- JD 附件上传
- 生成内容人工确认标记

## 10.3 P2 后续增强

- 英文简历
- 招聘网站 JD 自动抓取
- 简历投递记录
- 求职进度管理
- 面试题推荐
- 求职顾问模式
- 企业版能力
- 模板市场

---

## 11. 验收标准

## 11.1 登录

- 用户可以通过手机号验证码登录。
- 新手机号首次登录后自动创建账号。
- 未登录用户不能访问工作台和用户数据。

## 11.2 简历生成

- 用户可以手动创建一份中文简历。
- 用户可以上传 PDF / Word 简历并生成可编辑结构化内容。
- 用户可以选择 2-3 个模板中的任意一个。
- 简历可以正常预览为 HTML。
- 简历可以成功导出为 PDF。

## 11.3 JD 管理

- 用户可以创建、编辑、删除 JD。
- 系统可以展示 JD 列表和详情。
- 系统可以提取 JD 核心关键词。

## 11.4 简历评估

- 用户可以选择一份简历和一个 JD 发起评估。
- 系统可以输出总分和分项评分。
- 系统可以输出优势、不足、风险项和关键词覆盖情况。
- 系统可以输出具体优化建议。
- 每次评估结果会被保存。

## 11.5 一键优化

- 用户可以基于评估结果生成优化简历副本。
- 原简历不会被覆盖。
- 新简历与目标 JD、原简历、评估记录关联。
- 新简历可以继续编辑。
- 新简历可以导出 PDF。
- 优化历史会被保存。

---

## 12. 暂不包含范围

MVP 阶段暂不包含：

- 移动端完整适配
- 邮箱登录
- 密码登录
- 第三方登录
- 英文简历
- 自动投递简历
- 招聘网站账号登录
- 企业 HR 管理后台
- 付费系统
- 实时协作编辑
- 多模板市场
- 面试辅导完整模块

---

## 13. 待进一步确认问题

1. 手机验证码服务供应商选择。
2. 单个上传文件大小限制。
3. PDF 导出方案选择。
4. AI 模型服务选择。
5. 历史记录保留周期。
6. 是否需要用户手动确认 AI 修改后才保存优化简历。
7. 是否需要支持删除账号及清空个人数据。
